<h1>Please have a look at all the vans available</h1>

<ul>
  <% @vans.each do |van| %>
    <li><%= link_to van.title, van_path(van) %>

    <%= link_to "Delete?", van_path(van), method: :delete, data: { confirm: "Are you sure you want to delete this van ?" } %>
</li>
  <% end %>
</ul>

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-sm">
        <div class="card-body">
          <h1 class="card-title mb-4 text-center">Créer un nouveau van</h1>
          <%= render "form" %>
        </div>
      </div>
      <div class="text-center mt-3">
      </div>
    </div>
  </div>
</div>

<div class="container mt-5 text-center">
  <%= link_to "Back to Home page", root_path, class: "btn btn-outline-secondary"  %>
</div>
